#include <stdio.h>
#include <stdlib.h>
int main() {
    int taille ,i,el;

    printf("entre la taille:");
scanf("%d",&taille);
    printf("entre les elements:");
    scanf("%d",&el);


    for(int i=0;i<=taille;i++){

if()
}

return 0;

































































