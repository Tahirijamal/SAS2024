#include <stdio.h>
#include <stdlib.h>
int main() {
    int n;
    printf("entre la valeur de n :");
    scanf("%d",&n);
    if (n%2==0){
    printf("le nomber is ampaire");
   } else {
    printf("le nomber is enpaire ");
    } return 0;
}


